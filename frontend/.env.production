# In production, the backend serves both API and frontend from the same origin.
# The Vite proxy is dev-only; in production, /api calls go directly to the same server.
VITE_APP_NAME=Springdale School Management
VITE_APP_VERSION=1.0.0
