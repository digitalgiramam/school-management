{
  "version": 2,
  "buildCommand": "NODE_ENV=development npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend && npm install --prefix backend --legacy-peer-deps && npm run generate --prefix backend",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/api/:path*",  "destination": "/api/index.js" },
    { "source": "/health",      "destination": "/api/index.js" },
    { "source": "/api-docs/:path*", "destination": "/api/index.js" },
    { "source": "/:path*",      "destination": "/index.html" }
  ]
}
